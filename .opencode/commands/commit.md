---
description: Generate commit message following project standards
---

! git diff --staged
Read AGENTS.md for the commit message format defined in this project.

Based on the staged changes, generate a commit message following the format in AGENTS.md.

Then execute:
! git commit -m "<generated message>"
